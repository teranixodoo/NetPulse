# Zkopíruj do .env a doplň hodnoty
FERNET_KEY=vygeneruj: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
POSTGRES_PASSWORD=zvolsi_silne_heslo
JWT_SECRET=zvolsi_silne_heslo
